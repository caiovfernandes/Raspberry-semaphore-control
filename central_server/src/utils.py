def get_message(noturno, emergencia, normal, debug):
    message = {
        "modo_noturno": noturno,
        "modo_emergencia": emergencia,
        "modo_normal": normal,
        "modo_debug": debug
    }

    return message